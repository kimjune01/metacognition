# Phase 0a: Search Queries

Generated by Codex (GPT-5.4), session `019cfbbc-574c-79e1-89e6-1c02f264b2af`.
No framework context provided. Verbatim output below.

---

1. **File Organizer**
   Moves or sorts files into folders based on name, type, date, or other simple rules.
   `gh search repos "file organizer" --language=python --license=mit --sort=updated --limit=20`

2. **CSV Cleaner**
   Reads messy CSV files and fixes headers, blanks, duplicates, or formatting issues.
   `gh search repos "csv cleaner" --language=python --license=mit --sort=updated --limit=20`

3. **Log Parser**
   Scans log files and pulls out errors, warnings, counts, or other useful patterns.
   `gh search repos "log parser" --language=python --license=mit --sort=updated --limit=20`

4. **Backup Script**
   Copies files or folders to another location on a schedule or with simple versioning.
   `gh search repos "backup script" --language=python --license=mit --sort=updated --limit=20`

5. **Note Searcher**
   Searches through plain text or Markdown notes to find matching words, tags, or dates.
   `gh search repos "note search" --language=python --license=mit --sort=updated --limit=20`

6. **JSON Formatter**
   Reformats JSON files to make them easier to read, compare, validate, or clean up.
   `gh search repos "json formatter" --language=python --license=mit --sort=updated --limit=20`

7. **Duplicate Finder**
   Finds duplicate files by name, size, or hash so they can be reviewed or removed.
   `gh search repos "duplicate finder" --language=python --license=mit --sort=updated --limit=20`

8. **Folder Watcher**
   Monitors a folder and reacts when new files appear, such as renaming or moving them.
   `gh search repos "folder watcher" --language=python --license=mit --sort=updated --limit=20`

9. **RSS Reader**
   Fetches RSS feeds and shows, filters, or saves new items from websites and blogs.
   `gh search repos "rss reader" --language=python --license=mit --sort=updated --limit=20`

10. **Text Extractor**
    Pulls useful text from files, web pages, or documents for later searching or analysis.
    `gh search repos "text extractor" --language=python --license=mit --sort=updated --limit=20`
